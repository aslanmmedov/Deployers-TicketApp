const BASE_URL = "https://warm-west-marlin.glitch.me";

const endpoints = {
  events: "/events",
  students: "/venues",
  orders: "/orders",
  users: "/users"
};

export { endpoints };
export default BASE_URL;